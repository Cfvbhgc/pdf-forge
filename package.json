{
  "name": "pdf-forge",
  "version": "1.0.0",
  "description": "PDF generator from HTML templates using Puppeteer and Express. Supports invoice, report, resume, and certificate templates with variable substitution.",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "build": "echo 'No build step required for CommonJS project. Use docker build for production.' && exit 0"
  },
  "keywords": [
    "pdf",
    "generator",
    "puppeteer",
    "html-to-pdf",
    "templates",
    "invoice",
    "report",
    "resume",
    "certificate"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "puppeteer": "^23.4.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
